name: Deploy Web (development)

on:
  push:
    branches: [development]
  workflow_dispatch:

concurrency:
  group: deploy-web-development
  cancel-in-progress: true

jobs:
  deploy:
    name: Deploy to development
    runs-on: ubuntu-latest
    # Maintainer sandbox: no required reviewers. Declaring the environment is
    # required if the Azure token is scoped as an environment secret, and gives
    # GitHub deployment history + a surfaced URL.
    environment:
      name: development
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          lfs: false
      - name: Build And Deploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_MANGO_PLANT_0E878670F }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          deployment_environment: "development"
          app_location: "./"
          api_location: ""
          app_build_command: "npm run build:web"
          output_location: "apps/web/dist"
