; The shared @pianel/ui workspace and apps/desktop both consume @radix-ui/*,
; whose react-dom peer is not hoisted to the workspace root. npm's strict peer
; resolution rejects this; relax it so `npm install` succeeds across workspaces.
legacy-peer-deps=true
