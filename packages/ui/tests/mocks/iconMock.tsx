// Stub for `lucide-react/dist/esm/icons/*` default-exported icon components.
import * as React from 'react';

type IconProps = React.SVGProps<SVGSVGElement> & {className?: string};

const Icon = (props: IconProps) => React.createElement('svg', props);

export default Icon;
